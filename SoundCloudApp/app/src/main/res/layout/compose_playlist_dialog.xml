<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >
    <Button
        android:id="@+id/button_add_playlist"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:text="New Playlist"
        android:layout_margin="2dp"
        android:padding="5dp"
        android:textStyle="bold"
        >
    </Button>
	 <alburraq.gocloud.utils.IndexableListView
	    android:id="@+id/listView_playlists"
	    android:layout_width="fill_parent"
	    android:layout_height="wrap_content"
	    android:background="@android:color/transparent"
	    android:layout_below="@+id/button_add_playlist"
	    android:divider="@android:color/transparent"
	    android:scrollbars="none"></alburraq.gocloud.utils.IndexableListView>
</RelativeLayout>
